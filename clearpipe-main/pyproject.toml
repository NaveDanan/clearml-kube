[project]
name = "clearpipe-scripts"
version = "0.1.0"
description = "Python scripts for ClearPipe - ClearML integration"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "clearml>=1.14.0",
    "clearml-agent>=2.0.6",
]

[dependency-groups]
dev = []
