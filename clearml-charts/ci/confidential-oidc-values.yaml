# OIDC regression fixture: the ClearML API receives native OIDC configuration
# and the confidential client secret reference.
auth:
  mode: "oidc"
  oidc:
    enabled: true
    issuerUrl: "https://issuer.example.invalid/realms/clearml"
    redirectUrl: "https://clearml.example.invalid/api/login.oidc_callback"
    postLogoutRedirectUrl: "https://clearml.example.invalid/login"
    existingSecret: "test-oidc"
