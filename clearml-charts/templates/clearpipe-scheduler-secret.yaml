{{- if and .Values.clearpipeScheduler.enabled (not .Values.clearpipeScheduler.existingSecret) }}
{{- fail "clearpipeScheduler.existingSecret is required when the ClearPipe scheduler is enabled; pre-provision keys access_key and secret_key" }}
{{- end }}
